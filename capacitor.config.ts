import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.mattwong37.questengine',
  appName: 'QuestEngine',
  webDir: 'dist/quest-engine/browser'
};

export default config;
